<!DOCTYPE html>
<html>
<head>
    <title>{{ pageData.categoryInfo.title }}</title>
    <meta
</head>
<body>
    <h1>{{ pageData.categoryInfo.title }}</h1>
    <navigation>{{ navigation|raw }}</navigation>
    <content>
    <ul>
    {% for item in displayData %}
        <li>Název: {{ item.title }}, Snippet: {{ item.snippet }}, URL: <a href="http://{{ item.domain }}{{ item.url }}">http://{{ item.domain }}{{ item.url }}</a></li>
    {% endfor %}
</ul>

    </content>

</body>
</html>
